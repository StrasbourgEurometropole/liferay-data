Lors d'une livraison du thème par thuria, il y a quelques fichiers à ne pas écraser :

Les librairies js suivantes :

- js/libs/jquery.selectric.min.js
- js/libs/pikaday.js
- js/libs/owlcarousel2-a11ylayer.js
- js/src-1/bloc-pickaday.js
- custom/overide.js

Le point d'entré sass strasbourg.scss -> écraser le fichier mais recopier la dernière partie du fichier concernant custom.scss (@import "override/custom";)


Le fichier js/src-1/bloc-prefooter.js :
    le code suivant a été enlevé, déplacé et adapté dans le fichier pacte-view.jsp du module pacte-web

    $('.pro-bloc-prefooter .pro-signature-pacte > a').on('click',function(e){
        e.preventDefault();
        $(this).toggleClass('active');
        if($(this).hasClass('active')){
            $('h3',this).text('Vous avez adhéré au pacte');
            $('span',this).css('display','none');
            if($(this).hasClass('pro-disabled')){
                $('h3',this).text('Signer');
                $('span',this).css('display','block');
            }
        }
        else if($(this).hasClass('pro-disabled')){
            $('h3',this).text('Signer');
            $('span',this).css('display','block');
        }
        else{
            $('h3',this).text('Signer');
            $('span',this).css('display','block');
        }
    });

Le fichier js/src-1/bloc-vignette.js :
    Le code suivant a été commenté afin de gérer individuellement les likes/dislikes dans le fichier custom/overide.js

    $("[href$='like-pro']").on('click',function(e){
        e.preventDefault();
        $(this).text(+parseInt($(this).text()) + 1);
        $(this).toggleClass('active');
    });
    
    $("[href$='-approuv']").on('click',function(e){
        e.preventDefault();
        $(this).find('strong').text(+parseInt($(this).text()) + 1);
        $(this).toggleClass('active');
    });

Le fichier js/src/scipt.js
    Le code suivant a été commenté afin de gérer individuellement les actions sur les boutons d'intéraction

    $('.pro-bloc-card-event').on('click',function(e){
        e.preventDefault();
       $(this).find('pro-btn-action').toggleClass('active');
    });

    $('.pro-btn-signer').on('click',function(e){
        e.preventDefault();
       $(this).toggleClass('active');
    });


    // Call To Action -- Ajout de la Classe Active
    $('.pro-btn-action').on('click',function(e){
        e.preventDefault();
        e.stopPropagation();
        $(this).toggleClass('active');
    });

Le fichier js/src-1/bloc-pickaday.js
    Le code suivant a été complété afin de gérer la recherche en ajax

    // Utilisé pour les recherches ajax
    if(this._o.field.className.indexOf('dynamic') >= 0){
        // Renvoi la liste des entités demandées
        getSelectedEntries();
    }

Le fichier js/src-1/bloc-facette.js
    Le code suivant a été complété afin de gérer la recherche en ajax

    // Utilisé pour les recherches ajax
    if($(this).hasClass('dynamic')){
        // Renvoi la liste des entités demandées
        getSelectedEntries();
    }

